/*
 * Handlebars helpers are defined in this file.
 * See https://handlebarsjs.com/guide/#custom-helpers for more information.
 */

const helpers = {
  loud: function (aString) {
    return aString.toUpperCase()
  },
}
